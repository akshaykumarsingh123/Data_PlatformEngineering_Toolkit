verify disabled ls jobs 

--DISABLE backup jobs on primary

USE MSDB;
GO
UPDATE MSDB.dbo.sysjobs
SET Enabled = 0
WHERE [Name] LIKE 'LSBackup%';
GO

--DISABLE copy jobs on secondary:

USE MSDB;
GO
UPDATE MSDB.dbo.sysjobs
SET Enabled = 0
WHERE [Name] LIKE 'LSCopy%';
GO


--Disable Restore jobs on secondary:

USE MSDB;
GO
UPDATE MSDB.dbo.sysjobs
SET Enabled = 0
WHERE [Name] LIKE 'LSRestore%';
GO

--DISABLE Alert secondary jobs:

USE MSDB;
GO
UPDATE MSDB.dbo.sysjobs
SET Enabled = 0
WHERE [Name] LIKE 'LSAlert%';
GO